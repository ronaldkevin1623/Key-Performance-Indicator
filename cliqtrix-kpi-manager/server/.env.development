
# Server Configuration
NODE_ENV=development
PORT=5000
HOST=localhost
FRONTEND_URL=http://localhost:5173

# MongoDB Configuration
# IMPORTANT: Make sure /cliqtrix-kpi is included in the URI
MONGODB_URI=mongodb+srv://cliq:cliq2025@kpi.1bdy5g3.mongodb.net/cliqtrix-kpi?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=cliqtrix_super_secret_jwt_key_change_in_production_min_32_characters
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=cliqtrix_super_secret_refresh_key_change_in_production_min_32
JWT_REFRESH_EXPIRES_IN=30d

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# Logging
LOG_LEVEL=debug

# ================================
# IMPORTANT NOTES
# ================================
# 
# 1. The database name MUST be in the URI: /cliqtrix-kpi
# 2. Password is cliq2025 (without special characters)
# 3. If connection fails, check:
#    - Internet connection
#    - MongoDB Atlas IP whitelist (should be 0.0.0.0/0)
#    - Password is correct
#    - Database name is included